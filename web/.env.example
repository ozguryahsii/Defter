# Copy to .env and adjust. SQLite file lives next to the schema.
DATABASE_URL="file:./dev.db"

# Generate a strong value, e.g. `openssl rand -base64 32`
NEXTAUTH_SECRET="change-me"
NEXTAUTH_URL="http://localhost:3000"
